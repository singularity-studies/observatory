# Cases

No cases are defined.

Future case directories must use immutable case identifiers and contain a scope statement, identifier history, provenance references, and panel-membership history. A case directory must not imply inclusion in a Frozen Panel or the occurrence of any empirical event.
